apiVersion: v1
kind: Service
metadata:
  name: approval-processing-service
  namespace: erp
spec:
  selector:
    app: approval-processing-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
    - name: grpc
      port: 50051
      targetPort: 50051
      protocol: TCP
